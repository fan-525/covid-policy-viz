# 测试计划 — 病毒与政策：一场全球博弈

> 基于分工文档角色 B/C/D 验收项的系统功能验证

---

## 测试环境

- **项目路径**：`covid-policy-viz/`
- **测试工具**：Playwright v1.59
- **目标浏览器**：Chromium（Headless）
- **视口**：1280x720 和 1920x1080
- **启动方式**：`npx vite preview --port 4173`（基于已有 dist/ 静态预览）

---

## A. 页面加载与 UI 反馈

### A1 — 页面加载 loading 动画
- **验收标准**：打开页面 → 3 秒内出现 loading 动画，随后内容显示（角色 D-5/8）
- **操作步骤**：
  1. 导航到 `http://localhost:4173`
  2. 等待 `.loading-screen` 元素出现
  3. 等待 `.loading-screen` 元素消失（数据加载完成）
  4. 记录从页面打开到 loading 消失的时间
- **预期结果**：loading 出现后 3 秒内消失，主内容（`.hero-header`）可见
- **实际结果**：

### A2 — 引导提示淡出
- **验收标准**：首次访问出现"向下滚动开始探索"引导提示，3 秒后淡出（角色 D-5）
- **操作步骤**：
  1. 页面加载完成后检查 `.scroll-hint` 元素存在
  2. 等待 4 秒
  3. 检查 `.scroll-hint` 是否包含 `.fade-out` class（opacity: 0）
- **预期结果**：引导提示存在，3 秒后 opacity 变为 0
- **实际结果**：

### A3 — 粘性图表 + 滚动文字布局
- **验收标准**：UI 布局实现粘性图表 + 滚动文字，参照 MBTA Viz（角色 D-1）
- **操作步骤**：
  1. 检查 `.sticky-graphic` 元素存在且 `position: sticky`
  2. 检查 `.scroll-text` 元素存在
  3. 截图保存布局快照
- **预期结果**：左侧 `.sticky-graphic` sticky 定位，右侧 `.scroll-text` 固定宽度
- **实际结果**：

### A4 — 响应式布局（1280px 和 1920px）
- **验收标准**：在 1280px 和 1920px 宽度下正常，图表不溢出、文字不遮挡（角色 D-6）
- **操作步骤**：
  1. 视口设为 1280x720，截图保存
  2. 视口设为 1920x1080，截图保存
  3. 检查是否有元素溢出或重叠
- **预期结果**：两种宽度下布局正常，无溢出或重叠
- **实际结果**：

---

## B. 滚动叙事联动（上篇）

### B5 — 章节1：脉冲地图 + 时间定位
- **验收标准**：滚动到章节1 → 脉冲地图全屏显示，时间定位 2020/01-06（角色 B-5a）
- **操作步骤**：
  1. 页面滚动至 `#section1`
  2. 等待 `.step[data-step="1"]` 激活（含 `.is-active` class）
  3. 通过 `page.evaluate` 读取 `useGlobalStore.activeChart` 应为 `pulseMap`
  4. 检查 `store.currentTime` 在 2020/01-06 范围内
- **预期结果**：activeChart='pulseMap'，时间在 2020-01-12 附近
- **实际结果**：

### B6 — 章节2：时间线 + 韩国 vs 美/意对比
- **验收标准**：滚动到章节2 → 切换到时间线，默认高亮韩国（早期响应）vs 美国、意大利（晚期响应）（角色 B-5b）
- **操作步骤**：
  1. 滚动至 `#section2`
  2. 等待 `.step[data-step="1"]` 激活
  3. 读取 `store.activeChart` 应为 `timeline`
  4. 读取 `store.selectedCountries` 应包含 KOR/USA/ITA
- **预期结果**：activeChart='timeline'，selectedCountries 包含 KOR
- **实际结果**：

### B7 — 章节3：聚焦干预窗口 + 反事实滑块自动演示
- **验收标准**：滚动到章节3 → 时间线聚焦"最佳干预窗口"标注，反事实滑块自动演示（角色 B-5c）
- **操作步骤**：
  1. 滚动至 `#section3`
  2. 等待 `.step[data-step="2"]` 激活
  3. 检查 `.counterfactual-slider` 可见
  4. 检查反事实滑块值是否在变化（自动演示 -30→0）
  5. 检查图表中是否有 `.inflection-label` 元素（最佳干预窗口）
- **预期结果**：滑块可见且在自动移动，干预窗口标注可见
- **实际结果**：

---

## C. 脉冲地图交互（GlobalPulseMap）

### C8 — 地图渲染 ≥50 国边界
- **验收标准**：渲染至少 50 个国家边界（角色 B-2a）
- **操作步骤**：
  1. 滚动至 `#section1`
  2. 通过 `page.evaluate` 统计 SVG 中 `.country` path 元素数量
- **预期结果**：`.country` path 数量 ≥ 50
- **实际结果**：

### C9 — Canvas 粒子层 + 变异株颜色
- **验收标准**：Canvas 粒子层存在，颜色编码变异株（角色 B-2c）
- **操作步骤**：
  1. 在脉冲地图容器中检查 `<canvas>` 元素存在
  2. 通过 `page.evaluate` 确认 canvas 有 2D context
  3. 截图验证粒子渲染效果
- **预期结果**：Canvas 元素存在且可见
- **实际结果**：

### C10 — 缩放和平移
- **验收标准**：支持鼠标拖拽平移和滚轮缩放（角色 B-2d）
- **操作步骤**：
  1. 在地图容器区域执行 `mouse.wheel` 滚轮缩放
  2. 检查 SVG `<g>` 元素的 `transform` 属性变化
  3. 截图对比缩放前后
- **预期结果**：transform 属性包含 scale > 1 的值
- **实际结果**：

### C11 — 点击国家更新 store
- **验收标准**：点击国家 → store.selectedCountries 更新（角色 B-2e）
- **操作步骤**：
  1. 滚动至 `#section1`
  2. 等待地图渲染完成
  3. 通过 `page.evaluate` 获取 `.country` path 列表
  4. 点击一个已知数据存在的国家（如 USA 对应数字代码 840）
  5. 通过 `page.evaluate` 读取 `store.selectedCountries`
- **预期结果**：selectedCountries 包含被点击国家的 ISO 代码
- **实际结果**：

### C12 — 响应时间变化
- **验收标准**：时间推进时脉冲大小和粒子数量动态更新（角色 B-2f）
- **操作步骤**：
  1. 设置 `store.currentTime = '2020-01-12'`，截图保存
  2. 设置 `store.currentTime = '2020-04-15'`，等待 500ms，截图保存
  3. 对比两图脉冲圆环大小/粒子数量差异
- **预期结果**：时间变化后脉冲渲染有可观察的变化
- **实际结果**：

---

## D. 政策时间线与反事实推演（PolicyTimeline）

### D13 — 双区域图表结构
- **验收标准**：病例面积图（上）+ 政策堆叠面积图（下）（角色 B-3a）
- **操作步骤**：
  1. 滚动至 `#section2`
  2. 等待时间线 SVG 渲染
  3. 检查 SVG 中是否包含 `path`（病例面积）和至少 5 层政策 `path`（堆叠）
  4. 截图保存完整时间线
- **预期结果**：病例面积图 + 5 维度政策堆叠图均可见
- **实际结果**：

### D14 — 最佳干预窗口标注
- **验收标准**：自动标注竖线+标签（角色 B-3b）
- **操作步骤**：
  1. 滚动至 `#section3`，使用 focusMode
  2. 检查 SVG 中是否包含 `.inflection-band` rect 元素
  3. 检查是否存在文字"最佳干预窗口"
- **预期结果**：暖橙色竖条和标签可见
- **实际结果**：

### D15 — 虚线连接
- **验收标准**：关键政策出台日期与病例拐点之间有虚线连接（角色 B-3c）
- **操作步骤**：
  1. 在时间线 SVG 中查找 `stroke-dasharray` 属性的 `line` 元素
  2. 截图确认虚线可见
- **预期结果**：存在虚线连接元素
- **实际结果**：

### D16 — Mouse hover tooltip
- **验收标准**：鼠标悬浮显示当日详细数据（角色 B-3d）
- **操作步骤**：
  1. 在时间线图表区域执行 mousemove
  2. 等待 `.chart-tooltip` 元素出现
  3. 检查 tooltip 内容包含日期、病例数、政策强度
  4. 截图保存 tooltip
- **预期结果**：tooltip 弹出并显示完整数据
- **实际结果**：

### D17 — 响应 selectedCountries 和 timeRange
- **验收标准**：时间线随国家选择和时间的范围变化更新（角色 B-3e）
- **操作步骤**：
  1. 通过 `page.evaluate` 设置 `store.selectedCountries = ['KOR']`
  2. 检查时间线标题是否变为"韩国"
  3. 设置 `store.selectedCountries = ['USA', 'ITA']`
  4. 检查时间线是否更新
- **预期结果**：时间线标题和数据随国家切换而更新
- **实际结果**：

### D18 — 反事实推演滑块
- **验收标准**：拖拽滑块 -30~+30 天，模拟曲线实时叠加，视觉可区分（角色 B-4）
- **操作步骤**：
  1. 滚动至 `#section3` 步骤2
  2. 等待 `.counterfactual-slider` 出现
  3. 拖拽滑块到 -30 位置（使用 `page.locator('input[type="range"]').fill('-30')`）
  4. 截图保存模拟曲线
  5. 拖拽滑块到 +30，截图对比
- **预期结果**：模拟曲线（淡粉紫色）与实际曲线（红色）视觉上可区分
- **实际结果**：

---

## E. 下篇滚动叙事联动

### E19 — 章节4：雷达图 + Delta 波
- **验收标准**：滚动到章节4 → 雷达图展示 Delta 波特征，多国叠加对比（角色 C-5）
- **操作步骤**：
  1. 滚动至 `#section4`
  2. 等待雷达图 SVG 渲染
  3. 检查 `store.activeChart` 应为 `waveRadar`
  4. 检查 `store.currentWave` 应为 4
  5. 检查至少 3 个国家多边形存在
- **预期结果**：雷达图渲染，wave=4，至少 3 国多边形
- **实际结果**：

### E20 — 章节5：疫苗地图 + 时间定位
- **验收标准**：滚动到章节5 → 切换到地图，指标切为疫苗，时间定位 2021/01-12（角色 C-5）
- **操作步骤**：
  1. 滚动至 `#section5`
  2. 检查 `store.activeChart` 应为 `vaccinePulseMap`
  3. 检查 `store.currentTime` 在 2021 年范围内
  4. 检查 `store.timeRange` 为 2021/01-12
- **预期结果**：activeChart='vaccinePulseMap'，时间在 2021 年
- **实际结果**：

### E21 — 章节6：热力矩阵全屏
- **验收标准**：滚动到章节6 → 热力矩阵全屏（角色 C-5）
- **操作步骤**：
  1. 滚动至 `#section6`
  2. 检查 `store.activeChart` 应为 `policyHeatmap`
  3. 检查热力矩阵 5×5 格子渲染
- **预期结果**：activeChart='policyHeatmap'，25 个格子可见
- **实际结果**：

### E22 — 章节7（若有）
- **验收标准**：SEIR 模拟器或总结性图表
- **操作步骤**：
  1. 检查是否存在 `#section7`
  2. 若存在，记录其图表类型
- **预期结果**：共 6 个章节，不存在章节 7
- **实际结果**：

---

## F. 热力矩阵交互（PolicyHeatmap）

### F23 — 5×5 矩阵渲染
- **验收标准**：正确渲染 5×5 格子，横纵轴为 5 个政策维度（角色 C-2a）
- **操作步骤**：
  1. 滚动至 `#section6`
  2. 统计 SVG 中 rect 格子数量
  3. 检查横纵轴标签（封锁、检测、口罩、疫苗、边境）
- **预期结果**：25 个格子，横纵轴标签完整
- **实际结果**：

### F24 — Tooltip 悬浮
- **验收标准**：悬浮显示政策组合、涉及国家数、平均病例下降率（角色 C-2c）
- **操作步骤**：
  1. 在热力矩阵格子上触发 mousemove
  2. 等待 `.policy-heatmap-tooltip` 出现
  3. 检查 tooltip 内容
  4. 截图保存
- **预期结果**：tooltip 含组合名称、国家数、下降率
- **实际结果**：

### F25 — 点击格子触发联动
- **验收标准**：点击格子 → 时间线在 500ms 内高亮相应国家（角色 C-2d）
- **操作步骤**：
  1. 点击热力矩阵第一个格子
  2. 记录点击时间
  3. 通过 `page.evaluate` 检查 `store.mapHighlightCountries` 是否更新
  4. 记录从点击到状态更新的耗时
- **预期结果**：mapHighlightCountries 更新，延迟 < 500ms
- **实际结果**：

### F26 — 指标切换
- **验收标准**：支持至少两种指标切换，所有关联图表同步更新（角色 C-2e）
- **操作步骤**：
  1. 点击"死亡率下降率"按钮
  2. 检查 `store.currentPolicyMetric` 变为 `death`
  3. 检查热力矩阵颜色重新渲染
  4. 截图保存
  5. 点击"确诊下降率"，重复检查
- **预期结果**：指标切换后矩阵颜色和数据发生变化
- **实际结果**：

---

## G. 雷达图交互（WaveRadarChart）

### G27 — 五轴雷达图
- **验收标准**：五轴雷达图存在且数值可读（角色 C-3a）
- **操作步骤**：
  1. 滚动至 `#section4`
  2. 检查 SVG 中有 5 条轴线（line 从中心到外围）
  3. 检查 5 个轴标签（峰值、波次规模、爆发强度、持续时间、致命性）
  4. 截图保存
- **预期结果**：5 轴 + 5 标签完整
- **实际结果**：

### G28 — 多国叠加对比
- **验收标准**：至少 3 个国家叠加对比，不同颜色半透明（角色 C-3b）
- **操作步骤**：
  1. 确保 selectedCountries 至少有 3 个
  2. 检查 SVG 中 polygon 元素数量 ≥ 3
  3. 检查 polygon 的 fill-opacity < 1（半透明）
  4. 检查多边形颜色各不相同
- **预期结果**：≥3 个半透明多边形，颜色不同
- **实际结果**：

### G29 — 波次切换平滑过渡
- **验收标准**：波次切换按钮 + 平滑过渡动画（角色 C-3c）
- **操作步骤**：
  1. 点击"第2波"按钮
  2. 检查 `store.currentWave` 变为 2
  3. 检查 radar SVG 内容发生变化
  4. 点击"第3波"、"第4波"，重复
- **预期结果**：波次切换后雷达图形状变化
- **实际结果**：

### G30 — 悬浮数值 + 图例点击
- **验收标准**：鼠标悬浮显示具体数值，图例点击可取消/选择国家（角色 C-3d）
- **操作步骤**：
  1. 在雷达图多边形上触发 mouseenter
  2. 等待 `.radar-tooltip` 出现
  3. 检查 tooltip 内容含具体数值
  4. 点击图例中国家，检查 selectedCountries 变化
- **预期结果**：tooltip 展示数值，图例点击可切换国家
- **实际结果**：

### G31 — 响应 selectedCountries 和 currentWave
- **验收标准**：响应 store 状态变化（角色 C-3e）
- **操作步骤**：
  1. 通过 `page.evaluate` 添加一个新国家
  2. 检查多边形数量增加
  3. 切换 currentWave
  4. 检查雷达图更新
- **预期结果**：多边形数量随 selectedCountries 变化，图形随 wave 变化
- **实际结果**：

---

## H. 多视图双向联动

### H32 — 联动链路1：地图选国 → 雷达 → 时间线
- **验收标准**：地图点击国家 → 雷达图过滤该国波浪 → 时间线聚焦该国（角色 C-4）
- **操作步骤**：
  1. 滚动至 `#section1`，点击地图上一个国家
  2. 记录 `store.selectedCountries` 变化
  3. 滚动至 `#section4`，检查雷达图是否显示该国家
  4. 滚动至 `#section2`，检查时间线是否聚焦该国
  5. 记录操作到响应的延迟
- **预期结果**：三个视图同步显示同一国家，延迟 < 300ms
- **实际结果**：

### H33 — 联动链路2：热力矩阵 → 时间线高亮
- **验收标准**：热力矩阵点击政策组合 → 时间线高亮采用该组合的国家（角色 C-4）
- **操作步骤**：
  1. 滚动至 `#section6`
  2. 点击热力矩阵一个格子
  3. 记录时间
  4. 检查 `store.mapHighlightCountries` 更新且长度 > 0
  5. 滚动至 `#section1`，检查地图上高亮国家（橙色）
- **预期结果**：mapHighlightCountries 更新，地图上对应国家高亮
- **实际结果**：

### H34 — 联动链路3：指标切换同步
- **验收标准**：指标切换 → 所有图表同步切换指标（角色 C-4）
- **操作步骤**：
  1. 滚动至 `#section6`
  2. 切换指标为"死亡率下降率"
  3. 检查 `store.currentPolicyMetric` 更新
  4. 截图保存
- **预期结果**：热力矩阵颜色重渲染，反映新指标
- **实际结果**：

### H35 — 联动延迟 < 300ms
- **验收标准**：联动响应延迟 < 300ms（角色 C-4）
- **操作步骤**：
  1. 对 H32-H34 的每步联动，使用 `performance.now()` 记录操作开始时间
  2. 使用 `page.waitForFunction` 等待目标状态变化
  3. 记录延迟时间
- **预期结果**：所有联动延迟 < 300ms
- **实际结果**：

---

## I. 内容与文档

### I36 — 叙事文字检查
- **验收标准**：角色B 3章 + 角色C 3-4章，字数达标，衔接自然（角色 B-1, C-1）
- **操作步骤**：
  1. 提取所有 `.step p` 文本内容
  2. 统计总字数
  3. 检查章节间过渡文字
- **预期结果**：每章字数达标，章节过渡有引导语
- **实际结果**：

### I37 — about.html 内容完整性
- **验收标准**：包含研究问题、设计决策、数据源引用、开发流程，无错别字（角色 D-7）
- **操作步骤**：
  1. 获取 `docs/about.html` 文本内容
  2. 检查是否包含：研究问题、设计决策对比表、数据源 OWID/OxCGRT、
     参考案例、图表选择理由、团队分工、开发挑战
  3. 检查 HTML 结构完整性
- **预期结果**：所有必需板块齐全
- **实际结果**：

### I38 — linkage_test.md 存在
- **验收标准**：linkage_test.md 存在且记录联动测试结果（角色 C-6）
- **操作步骤**：
  1. 检查 `linkage_test.md` 文件存在
  2. 读取文件内容
  3. 检查是否包含联动链路表格和测试结果
- **预期结果**：文件存在，记录 8 条联动链路
- **实际结果**：

---

## 测试通过率汇总

| 分类 | 场景数 | 通过 | 失败 | 通过率 |
|------|--------|------|------|--------|
| A. 页面加载与 UI | 4 | | | |
| B. 上篇滚动联动 | 3 | | | |
| C. 脉冲地图 | 5 | | | |
| D. 政策时间线 | 6 | | | |
| E. 下篇滚动联动 | 4 | | | |
| F. 热力矩阵 | 4 | | | |
| G. 雷达图 | 5 | | | |
| H. 多视图联动 | 4 | | | |
| I. 内容文档 | 3 | | | |
| **总计** | **38** | | | |
